module.exports = {
    somar: (a, b) => Number(a) + Number(b),
    subtrair: (a, b) => Number(a) - Number(b),
    multiplicar: (a, b) => Number(a) * Number(b),
    dividir: (a, b) => Number(a) / Number(b),
  };
  

//  localhost:3000/somar/10/5 (retorna 15)
//localhost:3000/subtrair/10/5 (retorna 5)